@model IEnumerable<MvcPracownik.Models.Pudelko>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nazwa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.opis)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.cena)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.stan)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nazwa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.opis)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.cena)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.stan)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idpudelka">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idpudelka">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idpudelka">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
